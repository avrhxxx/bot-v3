============================================================
MASTER_MEMORY UPDATE – WORKFLOW API CONFIRMATION
============================================================

2026-02-23

1. Krok `/x sys setup` został utworzony i plik:
   src/commands/sys/setup.ts
   jest gotowy do użycia.

2. Nowy workflow dotyczący weryfikacji struktury repo przez API:
   - Za każdym razem, gdy trzeba się upewnić co do aktualnej struktury repo:
     1️⃣ Assistant prosi Usera o kliknięcie w zapisany link GitHub Tree API w notatniku.
     2️⃣ User klika link → GitHub zwraca aktualną strukturę w JSON.
     3️⃣ Assistant używa aktualnych danych do dalszych kroków (wskazanie plików, weryfikacja ścieżek).

3. Zasady ogólne pozostają bez zmian:
   - Wszystkie działania krok po kroku w Guided Command Mode
   - Memory tylko do odczytu; aktualizujemy je tylko przy istotnych zmianach workflow lub nowych krokach
   - Dispatcher, Ownership, `/x sys setup` i kolejne moduły trzymamy w pełnej zgodności z memory

============================================================
END OF MEMORY UPDATE
============================================================