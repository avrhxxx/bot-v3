{
  "name": "bot-v3",
  "version": "3.0.0-beta",
  "main": "dist/index.js",
  "scripts": {
    "generate-imports": "ts-node src/scripts/GenerateImports.ts",
    "build": "if [ \"$GENERATE_IMPORTS\" = \"true\" ]; then npm run generate-imports; fi && rm -rf dist && tsc",
    "start": "npm run build && node dist/index.js"
  },
  "dependencies": {
    "discord.js": "^14.14.1"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "ts-node": "^10.9.1"
  }
}