import { Guild, GuildBasedChannel } from "discord.js";

export const ShadowAuthorityUnit = async (
  guild: Guild,
  shadowRoleId: string,
  authorityIds: string[],
  notifyChannel?: GuildBasedChannel
) => {
  console.log("🔹 [ShadowAuthorityUnit] Placeholder task running...");
  // TODO: tu w przyszłości pełna logika synchronizacji i powiadomień
};